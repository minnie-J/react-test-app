import React from "react";

//NOTE: 셀 길이에 따라 컬럼 너비 일괄 적용
const Table = ({
  columnData,
  rowData,
  checkbox,
  border,
  header,
  height,
  onCheckedRows,
  onClickRow,
  onClickOrder,
}) => {
  return <div></div>;
};

export default Table;
